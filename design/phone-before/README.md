# LINEAGE on a phone, before the small-screen round

Every moment of `design/after/` on a phone held sideways (844 × 390 at 2× pixels), shot from main just before the small-screen round (after PR #21): the full generation panel, the iPad-sized narration, the story at 1× and no zoom. The same links, seed (6) and timings as `design/after/`, whose `*-phone.jpg` shots are the after.

Side by side: `design/compare.html`, "Phone".

*Made on 2026-09-26 with headless Chromium.*
